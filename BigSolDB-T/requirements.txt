# Core stack for the public workflow.
# Versions used for the reported results are noted where they matter.
#
# Notebooks 01-09 were originally run on torch 2.7.1; notebooks 10-12 and the
# matched MLP re-run on torch 2.13 (the PyTorch Geometric environment).
# See the reproducibility note in README.md.

numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8

torch>=2.7

# notebooks 10-11 only (GINE representation study)
torch-geometric>=2.5

# descriptor generation (notebook 02) and molecular graphs (scripts/graphs.py)
rdkit>=2023.9
